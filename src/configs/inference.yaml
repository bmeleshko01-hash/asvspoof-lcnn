defaults:
  - model: baseline
  - metrics: asvspoof
  - datasets: asvspoof
  - dataloader: main
  - transforms: main
  - _self_

inferencer:
  device_tensors:
    - data_object
    - labels
  device: auto
  save_path: "testing"
  seed: 1
from_pretrained: "saved/testing/model_best.pth"